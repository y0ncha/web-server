#include "request.h"

Request::Request(const std::string& raw) {
    // TODO: Parse request line, headers, and body
}

std::string Request::get_query_param(const std::string& key) const {
    // TODO: Parse query string (if exists)
    return "";
}